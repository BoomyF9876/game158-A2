#include "Collision.h"

bool COLLISION::SphereSphereCollisionDetected(const Body* body1, const Body* body2)
{
	// TODO for YOU
	// Instead of returning false all the time
	// Check if the distance between the two centres is bigger than the two radii
	// If so, no collision (return false)
	// Else, there is a collision (return true)
	return false;
}

void COLLISION::SphereSphereCollisionResponse(const Body* body1, const Body* body2)
{
	// TODO for YOU
	// Depends on weight and bounciness of the matter
	
}